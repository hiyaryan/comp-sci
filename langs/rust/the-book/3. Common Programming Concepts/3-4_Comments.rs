// Comments
// Comments are ignored by the compiler.
fn main () {
    // Below is a simple comment starting with two slashes '//'.
    // hello, world
    
    // Comments extending multiple lines need to inclue '//' on each line.
    // This is a confusing piece of code that needs multiple lines of comments
    // to explain which generally indicates something complex  has been written.
    
    let lucky_number = 7; // A comment can be placed at the end of lines of code.

    // More often comments are placed above code its annotating.
    let unlucky_number = 1;

    // Another type of commenting used for documentation is discussed in Chapter 14.
}
